<div class="row">
     <div class="col-md-3">
     </div>
     <div class="col-md-6">
           <form method="post" action="{{ path('frais_delete', {'id': frais.id}) }}" onsubmit="return confirm('etes vous sur de supprimé cet element ?');">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais.id) }}">
                <button class="btn btn-danger btn-block">Delete</button>
          </form>
     </div>
     <div class="col-md-3">
     </div>
</div>