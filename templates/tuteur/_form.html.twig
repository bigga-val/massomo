<div class="row">
    <div class="col-md-6">
        {{ form_start(form) }}
                {{ form_row (form.nom_complet, {attr:{'class':'form-control'}}) }}
                {# {{ form_row (form.profession, {attr:{'class':'form-control'}}) }} #}
                {# {{ form_row (form.adresseTuteur, {attr:{'class':'form-control'}}) }} #}
                {{ form_row (form.telephone, {attr:{'class':'form-control'}}) }}
                {# {{ form_row (form.telephoneBureau, {attr:{'class':'form-control'}}) }} #}
            
    </div>
      <button class="btn btn-outline-info btn-block" onclick="handleFormSubmission()">{{ button_label|default('Save') }}</button>
       {{ form_end(form) }}
</div>